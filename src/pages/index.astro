---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import { HOME_DESCRIPTION, HOME_TITLE, SITE_DESCRIPTION, SITE_TITLE, SITE_URL } from '../consts';
import {
	breadcrumbListSchema,
	organizationSchema,
	webPageSchema,
	webSiteSchema,
} from '../utils/schema';

const siteUrl = Astro.site?.href ?? SITE_URL;
const organizationJsonLd = organizationSchema(siteUrl, SITE_TITLE, SITE_DESCRIPTION);
const websiteJsonLd = webSiteSchema(siteUrl, SITE_TITLE, SITE_DESCRIPTION);
const webPageJsonLd = webPageSchema(siteUrl, siteUrl, HOME_TITLE, HOME_DESCRIPTION);
const breadcrumbJsonLd = breadcrumbListSchema(siteUrl, [{ name: 'Start', url: '/' }]);
---

<!doctype html>
<html lang="de">
	<head>
		<BaseHead title={HOME_TITLE} description={HOME_DESCRIPTION} />
		<script type="application/ld+json" set:html={JSON.stringify(organizationJsonLd)} />
		<script type="application/ld+json" set:html={JSON.stringify(websiteJsonLd)} />
		<script type="application/ld+json" set:html={JSON.stringify(webPageJsonLd)} />
		<script type="application/ld+json" set:html={JSON.stringify(breadcrumbJsonLd)} />
		<style>
			.hero {
				background: #010208;
				color: #FFFFFF;
				padding: 6rem 1.5rem 5rem;
				text-align: center;
			}
			.hero-inner {
				max-width: 720px;
				margin: 0 auto;
			}
			.hero h1 {
				font-family: "Montserrat", sans-serif;
				font-weight: 800;
				font-size: clamp(2rem, 5vw, 3.5rem);
				color: #FFFFFF;
				margin: 0 0 1rem 0;
				line-height: 1.15;
			}
			.hero p {
				font-family: "Source Sans 3", sans-serif;
				font-weight: 400;
				font-size: 1.15rem;
				line-height: 1.6;
				color: #FFFFFF;
				margin: 0 0 1.5rem 0;
			}
			.hero .cta {
				display: inline-block;
				padding: 0.75rem 1.5rem;
				background: #FFFFFF;
				color: #010208;
				font-family: "Montserrat", sans-serif;
				font-weight: 700;
				text-decoration: none;
				border: 2px solid #FFFFFF;
				border-radius: 0;
				transition: background 0.2s ease, color 0.2s ease, border-color 0.2s ease;
			}
			.hero .cta:hover {
				background: #010208;
				color: #FFFFFF;
				border-color: #FFFFFF;
			}
			.section-white {
				background: #FFFFFF;
				color: #010208;
				padding: 4rem 1.5rem;
			}
			.section-white .main-inner {
				max-width: 720px;
				margin: 0 auto;
			}
			.section-white h2 {
				font-family: "Montserrat", sans-serif;
				font-weight: 700;
				color: #010208;
			}
			.section-white a {
				color: #010208;
				text-decoration: underline;
			}
			.section-white a:hover {
				text-decoration: none;
			}
			.section-resources {
				background: #F5F5F5;
				color: #010208;
				padding: 4rem 1.5rem;
			}
			.resources-inner {
				max-width: 720px;
				margin: 0 auto;
			}
			.section-resources h2 {
				font-family: "Montserrat", sans-serif;
				font-weight: 700;
				color: #010208;
				margin: 0 0 1.5rem 0;
			}
			.resource-cards {
				display: grid;
				gap: 1.25rem;
				grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
			}
			.resource-card {
				display: block;
				background: #FFFFFF;
				color: #010208;
				text-decoration: none;
				padding: 1.5rem;
				border-radius: 8px;
				box-shadow: 0 2px 12px rgba(1, 2, 8, 0.08);
				border: 1px solid rgba(1, 2, 8, 0.06);
				transition: box-shadow 0.2s, border-color 0.2s;
			}
			.resource-card:hover {
				box-shadow: 0 4px 20px rgba(1, 2, 8, 0.12);
				border-color: rgba(234, 88, 12, 0.3);
			}
			.resource-card h3 {
				font-family: "Montserrat", sans-serif;
				font-weight: 700;
				font-size: 1.1rem;
				margin: 0 0 0.5rem 0;
			}
			.resource-card p {
				font-family: "Source Sans 3", sans-serif;
				font-size: 0.95rem;
				line-height: 1.5;
				margin: 0;
				color: #010208;
				opacity: 0.9;
			}
			.resource-card-img {
				display: block;
				width: 100%;
				max-height: 180px;
				object-fit: contain;
				object-position: top center;
				border-radius: 6px;
				margin-bottom: 1rem;
				background: #F5F5F5;
			}
		</style>
	</head>
	<body>
		<Header />
		<section class="hero" data-nav-invert>
			<div class="hero-inner">
				<h1>Ranknitro – SEO & GEO Agentur</h1>
				<p>{SITE_DESCRIPTION}</p>
				<p>Unser Fokus: organisches Wachstum durch Content. Mit dem Ranknitro-Blog liefern wir dir regelmäßig praxisnahe Tipps zu SEO, Google Ads, lokaler Suche (GEO) und Suchmaschinen-Marketing.</p>
				<a href="#geo-audit" class="cta">Free GEO Audit</a>
			</div>
		</section>
		<section class="section-resources">
			<div class="resources-inner">
				<h2>Ressourcen zum Download</h2>
				<div class="resource-cards">
					<a href="/geo-checkliste/" class="resource-card">
						<img src="/geo-checkliste-card.webp" alt="GEO-Checkliste Vorschau" class="resource-card-img" width="320" height="140" />
						<h3>GEO-Checkliste</h3>
						<p>Die 20 wichtigsten Kriterien für GEO/AEO – kostenloses PDF + GEO-Analyse.</p>
					</a>
					<a href="/seo-checkliste/" class="resource-card">
						<img src="/seocheckliste.webp" alt="47-Punkt SEO-Checkliste Vorschau" class="resource-card-img" width="320" height="140" />
						<h3>47-Punkt SEO-Checkliste</h3>
						<p>Technisches SEO, On-Page, Content & GEO, Local SEO, Monitoring – kostenlos in Google Tabellen.</p>
					</a>
				</div>
			</div>
		</section>
		<section class="section-white" id="geo-audit">
			<div class="main-inner">
				<h2>SEO & GEO aus einer Hand</h2>
				<p>Wir verbinden klassisches Suchmaschinen-Marketing mit GEO (Generative Engine Optimization): bessere Rankings in Google und Sichtbarkeit in KI-Systemen wie ChatGPT. <a href="/blog/">Im Blog</a> findest du praxisnahe Tipps; für individuelle Strategien erreichst du uns über die angegebenen Kontaktwege.</p>
			</div>
		</section>
		<Footer />
	</body>
</html>
